name: lcstats-relay
version: 0.0.0
description: APM-managed Agent Skills for LCStats Relay
author: aoirint
license: MIT
# Which agent platforms to deploy to.
# Resolution order: --target flag > this field > auto-detect from filesystem.
# Accepted values: vscode, agents, copilot, claude, cursor, opencode, codex,
# gemini, antigravity, windsurf, kiro, agent-skills, all
targets:
  - codex
dependencies:
  apm:
    - git: aoirint/skills
      ref: 0e41c088efb2bdf44ca58564bb5168d119ac9135
      skills:
        - apm-workflow
        - changelog-workflow
        - code-quality-check
        - commit-message-quality-check
        - flet-project-quality-check
        - git-worktree-workflow
        - github-actions-quality-check
        - github-workflow
        - gitignore-workflow
        - prose-quality-check
        - python-quality-check
        - release-note-workflow
        - security-check
        - domain-architecture-docs-workflow
        - test-quality-check
  mcp: []
includes: auto
scripts: {}
