name: Test source
description: Run the locked Python test suite with its coverage policy.

runs:
  using: composite

  steps:
    - name: Set up locked Python environment
      uses: ./.github/actions/setup-python

    - name: Run tests with full coverage
      shell: bash
      run: uv run --locked pytest
