[project]
name = "lcstats-relay"
version = "0.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
exclude-newer = "P7D"

[dependency-groups]
dev = [
    "mypy>=2.1.0,<3.0.0",
    "pytest>=9.0.3,<10.0.0",
    "ruff>=0.15.17,<0.16.0",
]
