interface:
  display_name: "GitHub Actions Quality Check"
  short_description: "Quality-check GitHub Actions workflows, pins, permissions, and CI linting."
  default_prompt: >-
    Use $github-actions-quality-check to review this GitHub Actions change for
    workflow syntax, action pinning, permissions, runner labels, triggers,
    concurrency, secrets, and verification coverage.
